<%= form_for(@part_bom) do |f| %>
  <% if @part_bom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@part_bom.errors.count, "error") %> prohibited this part_bom from being saved:</h2>

      <ul>
      <% @part_bom.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :part_id %><br>
    <%= f.text_field :part_id %>
  </div>
  <div class="field">
    <%= f.label :bom_item_id %><br>
    <%= f.number_field :bom_item_id %>
  </div>
  <div class="field">
    <%= f.label :quantity %><br>
    <%= f.number_field :quantity %>
  </div>
  <div class="actions">
    <%= f.submit '确认'%>
  </div>
<% end %>
