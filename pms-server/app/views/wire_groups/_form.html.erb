<%= form_for(@wire_group) do |f| %>
  <% if @wire_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wire_group.errors.count, "error") %> prohibited this wire_group from being saved:</h2>

      <ul>
      <% @wire_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :group_name %><br>
    <%= f.text_field :group_name %>
  </div>
  <div class="field">
    <%= f.label :wire_type %><br>
    <%= f.text_field :wire_type %>
  </div>
  <div class="field">
    <%= f.label :cross_section %><br>
    <%= f.text_field :cross_section %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
