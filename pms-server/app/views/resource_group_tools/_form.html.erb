<%= form_for(@resource_group_tool) do |f| %>
    <% if @resource_group_tool.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@resource_group_tool.errors.count, "error") %> prohibited this resource_group_tool from
            being saved:</h2>

          <ul>
            <% @resource_group_tool.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :nr %><br>
      <%= f.text_field :nr %>
    </div>
    <div class="field">
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <%= f.label :description %><br>
      <%= f.text_field :description %>
    </div>
    <div>
      <%= f.label :resource_group_part %><br>
      <% if @resource_group_tool.new_record? && @resource_group_tool.resource_group_part.blank? %>
          <%= f.text_field :resource_group_part %>
      <% else %>
          <%= f.text_field :resource_group_part, value: @resource_group_tool.resource_group_part.part.nr %>
      <% end %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
